name: mattermost_emoji_uploader
description: Reliable CLI for uploading custom emojis to Mattermost servers.
version: 1.0.0

environment:
  sdk: ^3.11.3

dependencies:
  args: ^2.7.0
  crypto: ^3.0.6
  http: ^1.5.0
  path: ^1.9.1
  sqlite3: ^2.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
